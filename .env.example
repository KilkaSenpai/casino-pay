# PostgreSQL connection used by Payload (`@payloadcms/db-postgres`).
# Matches the default credentials in docker-compose.yml.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/casino_pay

# Required secret for Payload auth/sessions. Use a long random string in production.
PAYLOAD_SECRET=change-me-to-a-long-random-string
